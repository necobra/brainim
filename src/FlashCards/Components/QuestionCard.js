// QuestionCard.js
import React from 'react';
import './QuestionCard.css';

const QuestionCard = ({ question, answers, onAnswerClick }) => {
    return (
        0
    );
};

export default QuestionCard;
